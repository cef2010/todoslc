<div class='container'>
<%= image_tag @activity.photo.url(:large) %>
<h1><%= @activity.name %></h1>
<h3><%= @activity.address %></h3>
<%= link_to "Add a Review", new_activity_review_path(@activity.id) %>
</div>
